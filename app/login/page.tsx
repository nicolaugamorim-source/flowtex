"use client";

import { LoginForm } from "@/components/ui/login-signup";

// Login route — just renders the shared LoginForm component.
export default function LoginPage() {
  return <LoginForm />;
}
